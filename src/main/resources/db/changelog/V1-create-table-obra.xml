<!-- <?xml version="1.0" encoding="UTF-8"?>   
<databaseChangeLog
   xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xmlns:pro="http://www.liquibase.org/xml/ns/pro"
   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.1.xsd
      http://www.liquibase.org/xml/ns/pro 
      http://www.liquibase.org/xml/ns/pro/liquibase-pro-4.1.xsd">

    <!-- O id deve ser único -->
    <!-- codigo, nome, endereco, data inicio, data prevista fim, data fim, custo previsto -->
   <changeSet  author="felipe.favato"  id="0">
      <createTable tableName="obra">
        <!-- ID -->
        <column  name="id"  autoIncrement="true" type="INTEGER">
          <constraints  nullable="false"  primaryKey="true"  unique="true"/>
        </column>
        <!-- NOME -->

        <column  name="nome"  type="VARCHAR(50)">
          <constraints  nullable="false"/>
        </column>

        <!-- ENDERECO -->
        <column  name="endereco"  type="VARCHAR(70)">
          <constraints  nullable="false"/>
        </column>

        <!-- DATA INICIO -->
        <column  name="data_inicio"  type="DATE">
          <constraints  nullable="false"/>
        </column>

        <!-- DATA PREVISTA FIM -->
        <column  name="data_prevista_fim"  type="DATE">
          <constraints  nullable="false"/>
        </column>

        <!-- DATA FIM -->
        <column  name="data_fim"  type="DATE">
          <constraints  nullable="false"/>
        </column>

        <!-- CUSTO PREVISTO -->
        <column  name="custo_previsto"  type="INTEGER">
          <constraints  nullable="false"/>
        </column>
      </createTable>
   </changeSet>

   <changeSet id="1" author="felipe.favato">
    <createSequence sequenceName="sq_obra" incrementBy="1" startValue="1"/>
   </changeSet>

</databaseChangeLog> -->